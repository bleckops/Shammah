# iOS app entry point

This directory is reserved for the Xcode project (`iosApp.xcodeproj`) that embeds the
Compose Multiplatform framework from `:composeApp`.

Expected files:
- `GoogleService-Info.plist` (environment-provided, not committed)
- Swift `@main` app bootstrapping `ComposeApp`
